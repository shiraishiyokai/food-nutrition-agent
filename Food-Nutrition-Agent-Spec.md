# Food Nutrition Agent — Product & AI Specification

**Version:** 0.1（Draft → 待评审）
**Status:** Spec 阶段，未开发
**Date:** 2026-09-22
**Project Code:** food-nutrition-agent（产品名待定，候选：食聊 / ChatDiet）
**Development Environment:** ZCode（AI 辅助开发）
**Primary Stack:** React + TypeScript + Capacitor (Android)
**Agent Type:** 对话驱动的多模态饮食营养记录与建议 Agent

> 上位文档：[AI-Agent-Portfolio-Plan.md](../agent-portfolio/AI-Agent-Portfolio-Plan.md)（总览规划，含方向决策记录）

---

# 1. 产品定位

## 1.1 一句话定位

> 一个**对话驱动**的个人饮食营养 Agent：拍照即记录，聊天即查询，档案自动养成——热量与营养估算、配料表解读、长期记录，全程几乎零手动输入。

## 1.2 核心理念（本项目最重要的产品决策）

**对话是唯一的操作入口；其余一切由 AI 自动生成填充。**

传统饮食 App 的失败模式是「记录负担」：用户要选食物、称分量、填表单，三天后弃用。本产品的回答：

| 传统 App 模式 | 本 Agent 模式 |
|---|---|
| 用户在食物库里搜索、选条目、填分量 | 拍张照片发进对话，AI 识别 + 估算分量，卡片确认即入库 |
| 用户手填个人资料表单 | 用户在闲聊中自然提及（「我乳糖不耐」「最近在减脂」），AI 自动抽取写入档案并口头确认 |
| 统计页需要用户主动去看 | AI 在对话中主动汇报今日/本周摄入状态 |
| 表单输入框 | 一个聊天框 |

用户体验对标：和一位懂营养的朋友在微信里聊天，这位朋友顺手帮你把账记了。

## 1.3 目标用户

- **主要**：作者本人（日常自用，每天 2~3 次高频验证）
- **次要**：关注饮食健康但懒得记卡路里的普通用户（朋友装机分发，验证「小产品」成色）

## 1.4 产品哲学

- **记录先行，教练 P2**：MVP 只做「记录 + 洞察」，不做目标打卡、督促、纠偏等教练功能；目标导向（赤字/盈余追踪）作为二期迭代
- **零表单**：任何需要用户「填」的东西都是设计失败；能从对话和照片推断的就推断，推断不确定就反问
- **本地优先**：数据在用户设备上，API Key 用户自己的（BYOK），产品本身零服务器、零运营成本

---

# 2. 决策记录（spec 编写期间确认）

| # | 决策点 | 结论 | 日期 |
|---|---|---|---|
| D1 | 产品定位 | 记录先行，教练功能 P2 | 2026-09-22 |
| D2 | 健康档案深度 | 含身体数据（身高/体重/年龄/性别/活动量，用于计算每日热量目标 TDEE）；体重定期打卡与趋势曲线二期做 | 2026-09-22 |
| D3 | 数据方案 | MVP 本地 SQLite；数据访问层做接口抽象，**架构预留云同步**，二期再上账号体系 | 2026-09-22 |
| D4 | 界面形态 | **对话为核心**；记录、档案、统计视图由 AI 依据对话内容自动生成填充，最小化用户输入（用户原话愿景，见 1.2） | 2026-09-22 |
| D5 | 营养数值来源 | **两段式架构**：VLM 只负责识别（菜名/分量/置信度），热量与三大营养素由本地营养库查表、代码计算——两条独立外部评审（Nutrition5K 论文 + 能力拆分分析）收敛结论，见 5.2 | 2026-09-22 |
| D6 | 本地化策略 | 本地分类器 + 端侧路由设为 **M4 决策门**（与 BYOK 引导页/轻代理三方对比后决策），M1~M3 不做任何本地模型集成 | 2026-09-22 |
| D7 | 营养数据模型 v2 | **原料级权威数值 + 菜品配方派生**：原料数值从 USDA SR Legacy 实拉（31 项，含 fdc_id 溯源；带鱼/面条用成分表公开值），菜品存配方（原料×克数），数值运行时计算；用户差异化 = 配方覆盖（"放了几克油两个蛋" → 数值重算，NL 解析 M3 落地）+ 热量直接校准双通道；配方为草案待校对 | 2026-09-22 |
| D8 | 数据源扩充 v3（双轨制） | 用户拍板：菜肴级采用**双轨制**——①直录轨：常见菜逐菜实拉薄荷食物库公开接口值（专业机构测算值，逐条带 source_url 溯源，个人自用不二次分发）；②派生轨：无参考值的菜保留配方派生，与直录值交叉校验、偏差>25% 标记复核。食材层扩至 78 项（62 USDA 实拉 + 14 成分表公开值/零值，后者带"待核对"标记）。**权威性边界如实声明：食材级有政府权威（USDA/成分表）；菜肴级全行业均为测算值，不存在政府权威表**。未命中菜品按类目均值兜底并显著标注"类目估算"（见 5.2 第三段） | 2026-09-24 |
| D9 | 数据源 v4：薄荷科学官方 API 按需补库 | 用户发现薄荷官方开放平台 [ai.boohee.com](https://ai.boohee.com)（160万+食物、含图片识别/饮食记录等企业级能力），注册免费档（个人可用：**7 个食物数据接口、50 次请求/天每日重置**；高级 API 如拍照识别不开放）。**批量枚举爬取路线废弃**（爬虫触发 429 限流，且网页版已下线、仅 JSON 接口存活）。新架构：本地库未命中的菜 → 官方 API 搜索（1 次调用即含每百克值+份量单位）→ **永久缓存**进本地库，此后零调用；带每日预算（软上限 45/50）与未命中永久负缓存。查找优先级：个人校准 > API 缓存 > 精选直录 > 配方派生 > 类目均值。**双 Key 问题的产品决策**：BYOK 双 Key 仅存在于 M1~M3 开发者自用阶段；M4 分发走轻代理（免费 Cloudflare Worker 档即可），用户零 Key、0 元/月；Key 绝不打进 APK。剩余限制：智能识别（拍照识别/自然语言识别）不在免费档，识别仍用 BYOK VLM（GLM-4V-Flash） | 2026-09-24 |
| D10 | 供应商配置全面开放 + 一句话修正分级解析 | 用户两项推动：①**供应商/模型必须允许用户自定义**——改为"预设只填默认值"：供应商预设（智谱/百炼/自定义/模拟）切换时预填接口根地址与模型名，**baseUrl、模型名、Key 全部可编辑**（任何 OpenAI 兼容 chat/completions 端点都能接），带**连通性测试**按钮（最小文本消息验证 地址+Key+模型，错误原样回显）；设置存档 v1→v2 自动迁移（Key 保留）。②**对话为核心不能只靠正则**——一句话修正升级为分级解析：规则层（本地正则，即时零成本，吃明确指令）→ 语义层（规则解析不了时**复用当前供应商配置**调文本模型转结构化操作 JSON，本地执行）；语义理解交给模型，数值与执行永远本地 | 2026-09-24 |
| D11 | M2 构建链定型：复用便携工具链 + Repository 数据层 | 复用 WoS_AI 便携工具链模式（Android SDK 复用 `WoS_AI/toolchain/android-sdk`，Gradle 走腾讯镜像）；新增自持 JDK 21（`toolchain/jdk-21`，TUNA 镜像下载，**Capacitor 8 要求 21**）并写入 `android/gradle.properties` 的 `org.gradle.java.home`；`local.properties` 指向便携 SDK，APK 构建不依赖系统全局环境。数据层按 6.1 落地 `MealRepo` 接口：web dev 走 localStorage，原生走 @capacitor-community/sqlite（meal_items 暂存 raw_json 单表，接口不变）；CapacitorHttp 开启后开发期代理路径（/ai/zhipu、/boohee）经 `resolveEndpoint` 映射回真实端点 | 2026-09-24 |
| D12 | 原生 SQLite 连接规约 + 模拟器验收路径 | ① `@capacitor-community/sqlite` 的 registerPlugin 代理对象**不能从 async 函数 return/await**（Promise 解析会访问 .then → 原生报 not implemented），封装 `data/sqlite.ts` 单例连接、resolve 普通包装对象，全 app 仅一次 createConnection；② 插件 `query()` 必须显式传 `values: []`；③ 模拟器验收：MuMu adb 端口 127.0.0.1:16384，`adb install -r app-debug.apk` + monkey 启动 + screencap 截图 + logcat 过滤 Capacitor/Console；adb input text 不支持中文（对话用 mock 英文问句验证） | 2026-09-24 |
| D13 | 双模型分工 + 多会话 + 设置收纳 | ① 识别用视觉模型（glm-4v-flash）、对话/语义修正用文本模型（glm-4-flash，更便宜），settings 增加 chatModel 字段（预设给 defaultChatModel，空则回落识别模型）——实测用户 Key 配好后对话仍走 mock 的观感问题与视觉模型误用同源暴露；② 对话多会话：fna.chat.v2 { sessions, activeId }，会话 chip 可切换/删除/新建，标题取首条消息；③ 设置卡收进识别页右上角 ⚙ 折叠（默认收起），header 显示当前供应商模式；对话页模式徽标（mock 时警示并可点击跳设置，App 层 fna:goto 事件） | 2026-09-24 |
| D14 | 个人校准持久化 + 备份迁移（用户自主换机） | ① 修复 K7 校准不持久：`upsertCalibration` 写 `fna.calib.v1`（localStorage，与薄荷缓存同模式），模块加载时自动恢复——原生端 WebView localStorage 持久已由 leveldb 取证证实，故不另建表；配方覆盖仍内存（无 UI 入口，观察中）。② 备份与迁移：`lib/backup.ts` 导出/导入设置/档案/餐记录/校准/薄荷缓存全部本地数据；web 走浏览器下载，原生写 Cache 目录 + @capacitor/share 系统分享；Key 是否随备份由用户勾选（脱敏规约：界面永不明文展示，不含 Key 的备份导入时保留目标机已有 Key）；MealRepo 增 `replaceAll`。E2E：校准→reload→仍生效；导出→清空→导入→全恢复；无 Key 备份不覆盖本机 Key。③ function calling 评估：对话查询升级原生 tools 时按 OpenAI 兼容格式一套代码通吃各供应商（行业已收敛），约半天，暂未实施 | 2026-09-24 |
| D15 | 对话查询升级真 function calling（D14③ 落地） | chat.ts 新增 `TOOLS`（query_today / query_week / lookup_food，OpenAI 兼容 tools 格式，一套代码通吃智谱/百炼/自定义端点）+ `executeTool`（模型只决定调什么传什么，数值计算全本地——两段式同一哲学）+ `answerQueryTools` 主循环（assistant tool_calls 原样回传、结果以 role:tool + tool_call_id 对应，最多 3 轮收敛）；ChatPage 优先走工具路径，任一失败 `catch` 回落 answerQuery 注入式（不锁死不支持 tools 的供应商）；气泡尾行 `🔧 工具名` 展示实际调用（回传历史时剥离，不进模型上下文）。**真 Key 实测**（Key 经 adb run-as leveldb 设备取证，全程不回显）：glm-4-flash 对「今天吃了多少热量」第一轮自主发出 `query_today`，第二轮基于本地回填数据回答，数字零编造；假 Key 验证 401→回落链干净降级无崩溃 | 2026-09-24 |

---

# 3. 核心场景与用户故事

## 场景 1：三餐拍照记录（P0，核心高频）

```
用户：（拍摄午餐照片发送）
Agent：（识别卡片）
      🍚 米饭 约200g ｜ 232 kcal ｜ 蛋白6g 脂肪1g 碳水50g
      🍅 番茄炒蛋 约150g ｜ 162 kcal ｜ 蛋白8g 脂肪11g 碳水7g
      🍗 炸鸡腿 1个(约120g) ｜ 312 kcal ｜ 蛋白20g 脂肪20g 碳水12g
      — 合计约 706 kcal
      ⚠️ 分量是估算值：米饭看起来像一碗(200g)，如果不到一碗告诉我
用户：米饭只有半碗
Agent：好，米饭改为100g → 这餐合计约 582 kcal。今天已摄入 1240 kcal。
```

要点：识别结果以**结构化卡片**呈现在对话流中；分量/品名不确定时 AI 主动反问（低置信度策略，见 5.3）；用户的一句话修正即时改写入库。

## 场景 2：配料表/营养成分表解读（P1）

```
用户：（拍摄某零食配料表照片）我在减脂，这个能吃吗？
Agent：配料表前三位是小麦粉、白砂糖、棕榈油——糖和油排很前，每100g有486kcal、
      含反式脂肪风险（部分氢化油）。你的档案显示目标减脂：
      偶尔解馋可以，建议单次 ≤ 30g，或者换成配料表第一位是全麦的款。
```

要点：解读必须**结合个人档案**（目标/过敏/忌口），这是通用 LLM 平替做不到的闭环点。

## 场景 3：对话式查询（P0）

```
用户：今天蛋白质够了吗？
Agent：今天截至现在共 3 餐 86g 蛋白质。按你的体重 72.5kg 和减脂目标，
      建议每天 110~130g，还差约 30g——晚餐加一份鸡胸或两个鸡蛋就能补上。
```

## 场景 4：档案自动养成（P1，差异化核心）

```
用户：对了，我最近花生过敏，别再推荐花生酱了
Agent：已记入档案（过敏：花生）。之后所有建议都会避开花生及花生制品。
      （后台：profile.allergies += ["花生"]，落库并更新 system prompt）
```


```

## 场景 6：体重打卡（P2）

```
用户：今天早上 72.3
Agent：已记录。比上周 -0.4kg，趋势健康。（二期：趋势曲线视图）
```

---

# 4. 功能规格

## 4.1 P0（MVP 必做）

| 功能 | 说明 |
|---|---|
| F1 对话主界面 | 消息流 UI：文本、图片（拍照/相册）、识别卡片、统计卡片；本地保存历史 |
| F2 拍照识别入库 | 图片 → VLM 识别（菜名/分量/置信度）→ 营养库查值得数值（两段式，schema 见 5.2）→ 卡片展示 → 确认/修正后落库 |
| F3 低置信度反问 | 任一 item 置信度 < 0.6 时，AI 生成针对性澄清问题而非硬猜 |
| F4 个人档案 | 忌口/过敏/偏好/目标 + 身体数据字段（D2）；MVP 手动编辑入口保留（设置页），但设计上以对话自动养成为主 |
| F5 对话查询 | 「今天/本周吃了多少」类查询，基于本地记录聚合 + 档案上下文回答 |
| F6 每日汇总视图 | 只读的自动生成视图（今日时间线、三大营养素环形图）——从对话/识别数据自动渲染，非用户输入 |
| F7 设置页 | BYOK：API Key（智谱/百炼）、模型选择、档案手动编辑、数据管理 |

## 4.2 P1

| 功能 | 说明 |
|---|---|
| F8 配料表深度解读 | 场景 2；配料顺序解读、添加剂风险提示、结合档案给建议 |
| F9 档案自动抽取 | 每轮对话后从用户消息抽取档案变更（结构化 side-effect，见 5.4） |
| F10 TDEE 每日热量目标 | 由身体数据自动计算参考目标，在查询回答中引用（不做打卡督促） |

| F12 数据导出/导入 | JSON 一键备份/恢复（换机兜底，D3 云同步落地前的主要保障） |

## 4.3 P2（二期）

| 功能 | 说明 |
|---|---|
| F13 目标导向教练 | 赤字/盈余打卡、偏离提醒、纠偏建议（D1 推迟的教练功能） |
| F14 体重趋势曲线 | 体重打卡记录 + 图表 |
| F15 云同步账号体系 | D3 预留接口的落地 |
| F16 主动摘要 | 每日晚间主动生成当日小结（需通知权限，Android 本地通知） |

---

# 5. AI 设计

## 5.1 模型选型（待 M1 实测定稿）

| 用途 | 首选 | 备选 | 说明 |
|---|---|---|---|
| 图像识别（餐食/配料表） | 智谱 GLM-4V-Flash | 阿里百炼 qwen-vl-plus | 免费档起步，控制成本；中餐识别效果需实测 |
| 对话/查询/抽取 | 智谱 GLM-4-Flash（文本） | 百炼 qwen-turbo | 与视觉模型同 Key 同 SDK，减少集成面 |

选型原则：国内直连（无梯子依赖）、免费额度起步、同一供应商覆盖视觉+文本。M1 用 20 张自摄餐照做 A/B 实测后锁定。

**职责边界（D5，2026-09-22 修订）**：VLM 只负责识别（菜名/分量/置信度），**不再输出任何营养数值**——数值由本地营养库查表（见 5.2）。因此模型选型只影响识别质量，数值质量由数据资产决定。

## 5.2 两段式架构：识别输出与营养数值查表（2026-09-22 修订）

> 依据：Nutrition5K VLM 评测（Wang et al. 2026, Curr Res Food Sci）及多项独立研究显示，VLM 直接估算营养数值误差大（最好模型 ~24%，多数 >40%），「让模型算数」是结构性误差源。收敛结论：**模型负责「这是什么菜、大约多少」，数据库负责「到底多少卡」**。

### 第一段：VLM 识别输出（强制 JSON，不含营养数值）

```json
{
  "meal_type": "lunch",            // breakfast|lunch|dinner|snack，可由时间推断
  "items": [
    {
      "name": "番茄炒蛋",           // 规范化常见菜名
      "category": "家常菜",         // 九类之一，未命中库时用于类目兜底估算
      "portion_desc": "约150g",
      "portion_g": 150,
      "confidence": 0.82           // 0~1，低于阈值触发反问
    }
  ],
  "notes": ["米饭按一碗200g估算，用户修正为100g"]   // 估算假设，随卡片展示
}
```

识别不出条目命名为「未知菜品」且 confidence < 0.4；禁止编造菜名。

### 第二段：本地营养库查表 + 代码计算

- 每道菜一条记录：每百克值（kcal/三大营养素）+ 典型份量 + 份量预设（一碗/个 → 克）；
- `数值 = 每百克值 × 克数 ÷ 100`，由代码计算：可审计、跨会话一致（不漂移）；
- 库中查不到的菜：数值列显示「——」并标注「待校准」，**禁止降级为 VLM 估算**；
- **个人校准闭环（对话式，差异化核心）**：用户说「这个菜其实 350 大卡」→ 写入个人校准条目，优先于默认库值——现有 App 有库但没有对话式校准。

数据资产：中文常见菜**配方表**（菜品 → 原料及克数 → 每百克值）。注意《中国食物成分表》按**原料**组织，菜品与原料间需要配方表映射；**原料数值不手写**——由 `scripts/fetch-usda.mjs` 从 USDA SR Legacy 数据集自动匹配实拉（64/67 命中），`scripts/build-ingredients.mjs` 装配，全程可溯源。配方克数为草案（油糖单列，用户校准优先调这两项）。版权注意：成分表数据不得原样再分发，自托管校准使用合规（见待验证清单）。

**数据源 v3 双轨制（D8，2026-09-24）**——M1 实测 9 餐后确认瓶颈仍是覆盖率，扩充为三层：

1. **直录轨（主力）**：~180 道常见菜的每百克值逐菜实拉自薄荷食物库公开接口（`scripts/fetch-boohee.mjs` + 精化/人工核定映射 → `dishes-reference.json`，每条带 source_url 溯源）。品牌预包装条目（"盒马/正点 × 菜名"）一律拒绝；匹配菜名经人工核定。
2. **派生轨（兜底补充）**：直录未覆盖的菜走配方派生（`dishes.json`）；`scripts/verify-dishes.mjs` 将派生值与直录值交叉比对，偏差 >25% 列为待复核。
3. **类目兜底（最后防线）**：库中无此菜时，按该菜 VLM 输出的 category（九类：家常菜/蔬菜/凉菜/汤/蛋白类/主食/外食/饮品/水果）取库内同类均值估算，UI 显著标注「类目估算」并提示校准——不冒充精确值，也不再大面积出现「—」。

查找优先级：个人校准 > 直录参考值 > 配方派生 > 类目均值。prompt 同步加入易混菜判别规则（鱼香肉丝 vs 京酱肉丝、豆腐生熟形态、带鱼炸/烧等），并要求输出 category。

**M1 实测阶段性结果（2026-09-22，真实 Key 4 餐 16 项）**：菜名识别 16/16 正确（含别名链 白饭→米饭）、分量推断合理、置信度校准准确（低置信项正确触发标记）；时延 3~5 秒、单次约 1000 tokens。**瓶颈确认为库覆盖率**（9/16 待校准）→ 催生 D7 数据模型 v2，实测缺失的 9 道家常菜已全部补入配方表。

**M1 实测第二批（2026-09-24，5 餐 21 项）**：别名/精确匹配 16 项、待校准 5 项（京酱肉丝[实为鱼香肉丝误识]、炒白菜、白灼青菜、香菇滑鸡、炸带鱼）；暴露两类问题：①易混菜视觉混淆（鱼香肉丝→京酱肉丝）→ prompt 判别规则；②库覆盖仍不足（单餐最多 3/4 项缺库）→ 催生 D8 双轨扩充。

实现：response_format/json mode + schema 校验（zod），校验失败自动重试一次（附带错误信息），两次失败降级为纯文本回答并提示用户稍后重试。

## 5.3 低置信度交互策略

- `confidence < 0.6` 的 item：不直接入库，AI 生成澄清问题（「这份看起来是糙米饭还是白米饭？大概小半碗还是一碗？」）
- 全部 item 确认后一次性落库；用户的一句话修正（「米饭半碗」）走轻量文本调用更新对应字段
- 分量默认假设必须显式写在 `notes` 里展示给用户，不允许黑箱估算

**一句话修正引擎（M1 已落地，2026-09-24）——D4"对话为核心"的第一块**：照片里菜多识别不准是常态，产品答案是让用户一句话改。结果卡片下方常驻修正输入框，分级解析：

1. **规则层**（本地正则，即时零成本）：吃掉明确指令——改名（"不是A是B"/"A改成B"）、分量（"N克"/"一半"/"两倍"/碗份预设）、删除、补充（自动取库中典型份量）、热量校准（"其实350卡"→按当前分量折算每百克写入个人校准库，永久生效）
2. **语义层**（GLM-4-Flash 免费文本模型）：规则解析不了的原句 → 结构化操作 JSON（rename/portion/delete/add/calibrate，zod 校验）→ 本地引擎统一执行。语义理解交给模型，数值与执行永远留在本地

修正过的条目标「已确认」徽章；改名后自动重新查库并触发薄荷 API 按需补库。多轮对话编排（省略主语、跨餐追问、主动澄清）仍归 M3 对话层。

## 5.4 对话层与档案抽取

**System prompt 组装顺序**（每轮动态构建）：

```
[角色] 你是用户的私人营养助手……（输出风格：简短、口语、不啰嗦）
[档案] 目标：减脂；过敏：花生；忌口：香菜；偏好：少油；
       身体数据：175cm/72.5kg/25岁/男/中等活动量；TDEE目标：1800kcal（P1）
[今日状态] 截至现在已摄入 1240 kcal（蛋白 62g/脂肪 48g/碳水 130g）
[规则] 1) 识别结果展示后等待确认再计入…… 2) 不得提供医疗诊断……
```

**档案自动抽取（F9）**：每轮用户消息后，附带一次轻量结构化抽取（可与主回复同次调用，输出增加 `profile_updates` 字段）：

```json
{ "reply": "……", "profile_updates": { "allergies": { "add": ["花生"] } } }
```

抽取结果先以一句话向用户确认（「已记入档案」）再生效，避免误抽取污染档案。

**上下文成本控制**：对话历史不全文携带——超过 20 轮后摘要压缩；查询类问题只注入「档案 + 当日/本周聚合数据」（本地 SQL 聚合，不喂原始记录）。

## 5.5 Token 与图片成本控制

- 图片上传前客户端压缩：长边 ≤ 1024px、质量 80（识别够用，Token 减半以上）
- 识别缓存：相同图片哈希 24h 内不重复调用
- 免费额度优先：GLM-4V-Flash / GLM-4-Flash 均有免费档；额度耗尽提示用户切换 Key 或模型

---

# 6. 技术方案

## 6.1 架构

```
┌─ Android APK（Capacitor 壳）─────────────────┐
│  React + TS 前端                              │
│   ├─ 对话主界面（消息流/图片/卡片）            │
│   ├─ 每日汇总视图（只读，自动生成）            │
│   └─ 设置页（BYOK/档案/数据管理）             │
│  ─────────────────────────────                │
│  AI 服务层（独立模块 ai/）                     │
│   ├─ vlm.ts     视觉识别（JSON schema）       │
│   ├─ chat.ts    对话（system prompt 组装）    │
│   └─ extract.ts 档案抽取                      │
│  ─────────────────────────────                │
│  数据层 data/（Repository 接口抽象 ← 预留云同步）│
│   └─ SQLite（Capacitor 社区插件）              │
└──────────────┬────────────────────────────────┘
               │ CapacitorHttp（绕 CORS，原生请求）
               ▼
      智谱 / 百炼 OpenAI 兼容端点（BYOK）
```

要点：
- **CapacitorHttp** 走原生 HTTP，规避 WebView CORS 限制（直连 LLM 端点的关键）
- **Repository 接口**（`MealRepo` / `ProfileRepo` / `ChatRepo`）：MVP 只有 SQLite 实现，二期加云端实现（D3）
- 纯浏览器（dev 模式）下用 fetch + 供应商 CORS 支持情况兜底，开发调试不依赖真机

## 6.2 数据模型（SQLite）

```
meals        (id, date, meal_type, photo_path, raw_json, created_at)
meal_items   (id, meal_id, name, portion_g, calories, protein_g, fat_g, carbs_g, confidence)
profile      (key, value, updated_at)          -- 单行 JSON 亦可
weight_logs  (id, date, weight_kg)             -- P2
chat_messages(id, role, content, meta_json, created_at)
food_cache   (photo_hash, result_json, created_at)
```

## 6.3 工程结构（AI 辅助开发友好）

```
src/
  ai/        # AI 服务层：与 UI 解耦，纯函数化，可单测
  data/      # Repository 接口 + SQLite 实现
  ui/        # 页面与组件（对话流、卡片、图表）
  app/       # 路由、全局状态
```

模块边界清晰是「AI 辅助开发」的生存策略：每块能被独立描述、独立验证（ai/ 层可先在浏览器里用测试页跑通再接 UI）。

---

# 7. 成本估算

| 场景 | 调用量 | 单价量级 | 月成本 |
|---|---|---|---|
| 三餐识别（免费档内） | 90 次/月 | GLM-4V-Flash 免费 | 0 元 |
| 对话+查询 | ~300 次/月 | GLM-4-Flash 免费档 | 0 元 |
| 免费额度耗尽后（按付费档） | 同上 | 视觉 ~0.01-0.05 元/次 | 约 3~15 元 |

结论：BYOK 模式下自用月成本 0~15 元，符合总览「百元内试水」约束；朋友各自用自己的 Key，产品零运营成本。

---

# 8. 风险与边界

| 风险 | 应对 |
|---|---|
| 中餐混合菜品/分量识别准确率不足 | M1 用 20 张自摄餐照实测；notes 强制展示估算假设；低置信度反问兜底；产品话术明确「估算非精确」 |
| 健康建议边界 | 系统规则：不诊断、不开药、慢病话题引导就医；界面免责声明一句话 |
| 隐私 | 数据全部本地；照片会发送至所选 VLM 供应商（设置页明示，可选供应商） |
| 免费额度政策变动 | 抽象 provider 层，一键切换智谱/百炼；BYOK 天然分散风险 |
| 换机丢数据 | P1 的 JSON 导出/导入；P2 云同步（D3） |

---

# 9. 里程碑（无 deadline，断点续做友好）

| 里程碑 | 交付物 | 独立验证标准 |
|---|---|---|
| **M1 能识别** | 浏览器测试页：上传/拍照 → 压缩 → VLM 识别 → 查表 → 卡片；prompt 初版 + 配方表首批 | **双指标验收**（2026-09-22 修订）：① 菜名识别准确率 ≥ 80%；② 分量猜测在合理区间（±30%）。营养数值来自营养库，不作为 VLM 指标单独统计。**2026-09-24 用户决定：不再卡 20 餐验收，M1 视为通过**（4 餐截图实测识别大体正确，识别误差走一句话修正兜底） |
| **M2 能记录** ✅ 开发完成（2026-09-24） | Capacitor APK + SQLite + 对话历史 + 每日汇总视图 | 手机上连续 3 天真实记录三餐（**待真机执行**） |
| **M3 能对话** ✅ 开发完成（2026-09-24） | 档案注入 system prompt + 对话查询 + 低置信度反问修正流 | 场景 1/3 完整走通（mock 模式浏览器+MuMu 双端走通；真实 Key 端待用户验收） |
| **M4 小产品化** | 配料表解读（场景2）、档案自动抽取（场景4）、外卖比价（场景5）、导出导入、设置页完善 | 发 APK 给 1~2 位朋友独立用起来；**触发「本地化决策门」（D6）**：凭真实使用数据（朋友 Key 卡点率、月调用量、配方表覆盖度、离线需求）做 BYOK 引导页 / 轻代理 / 本地分类器+配方表 三方对比后再定分发形态 |

每个里程碑结束时更新本文档的「决策记录」章节。

---

# 10. 非目标（明确不做）

- 外卖平台自动爬价（无公开 API，灰色地带；只做手动输入比价）
- 医疗诊断、疾病治疗建议
- iOS 版（MVP 只出 Android APK，验证后再议）
- 主动推送/督促类通知（P2 再议，MVP 不做）
- 食物数据库全覆盖式搜索（传统 App 路线，与「对话+识别」理念相悖）

---

# 11. 待验证清单（M1 专项）

1. GLM-4V-Flash 免费额度上限与限流策略实测
2. 智谱/百炼端点在 CapacitorHttp 原生请求下的兼容性
3. 中餐识别准确率基线（20 张自摄餐照，GLM-4V-Flash vs qwen-vl 对比）
4. JSON mode + schema 校验的稳定性（失败率、重试策略有效性）
5. 图片压缩参数（1024px/质量80）对识别准确率的影响
6. 档案抽取与主回复合并在同次调用的输出稳定性（`profile_updates` 字段丢失率）
7. 营养数据库选型与合规：《中国食物成分表》数据的版权边界（自托管 vs 再分发）、USDA FDC 对中餐的覆盖度、配方表数据来源与校对流程
8. （M4 决策门变量）onnxruntime-web 在 Android WebView 跑量化分类器的可行性；ChineseFoodNet 等闭集数据集的权重质量与许可——M4 前不投入
